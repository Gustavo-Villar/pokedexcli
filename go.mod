// This file is the Go module definition for the Pokedex CLI application.
// It defines the module path and specifies dependencies and their versions.

module github.com/gustavo-villar/pokedexcli // The module path used for import statements within the project.

go 1.20 // Specifies that the module is compatible with Go version 1.20. This ensures the use of features and behavior consistent with that Go version.
